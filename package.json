{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross": "^1.0.0",
    "express-validator": "^7.2.1"
  }
}
